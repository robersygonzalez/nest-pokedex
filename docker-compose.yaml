version: '3'

services:
  db:
    image: mongo:5
    restart: always
    ports:
      - 27017:27017
    environment:
      # Db Name
      MONGODB_DATABASE: nest-pokemon
    volumes:
      # Persists data to a named volume on the host
      - ./mongo:/data/db